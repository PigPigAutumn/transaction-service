apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  labels:
    app: transaction-service
spec:
  selector:
    app: transaction-service
  ports:
    - port: 10100
      targetPort: 10100
      protocol: TCP
  type: ClusterIP